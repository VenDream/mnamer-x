import { Button } from '@/components/ui/button';

export default function Settings() {
  return (
    <div className="flex h-full w-full p-4">
      <Button variant="outline">Settings Page</Button>
    </div>
  );
}
